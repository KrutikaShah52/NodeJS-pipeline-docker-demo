node{
    def app
    def tag = "latest"

    stage('Checkout'){
        checkout scm
    }

    stage('test') {
        def myTestContainer = docker.image('node:4.6')
        myTestContainer.pull()

        myTestContainer.inside {
            sh 'npm install'
            sh 'npm test'
        }
    }
}
